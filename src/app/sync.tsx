import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function SyncScreen() {
  return (
    <PlaceholderScreen
      title="Sincronizar"
      description="Enviá y recibí datos con el servidor."
      icon="sync"
      colorToken="accent"
      softToken="accentSoft"
    />
  );
}
